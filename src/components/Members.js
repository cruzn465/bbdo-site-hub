import React from "react";

function Members({ mediaObj, allMembers }) {
  // console.log("memInfo: ",memInfo)
  // console.log(memInfo.id, name, pronouns, img)
  return;
  // <div>This is a Members Comp</div>;
}

export default Members;
