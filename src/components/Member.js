import React from "react";

function Member({memInfo}) {
    // const {id, name, pronouns, img} = memInfo
    console.log("memInfo: ",memInfo)
    // console.log(memInfo.id, name, pronouns, img)
    return (
        <div>This is a Member Comp</div>
    );
}

export default Member;